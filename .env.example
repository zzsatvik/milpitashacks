# OpenAI API key for GPT-4o Vision lawn analysis (server-side only)
OPENAI_API_KEY=sk-...

# Set to "true" to skip the API and use demo mock data
# VITE_USE_MOCK=true

# Optional: override API URL (defaults to same origin / vite proxy)
# VITE_API_URL=http://localhost:3001
